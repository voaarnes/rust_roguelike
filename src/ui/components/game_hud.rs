use bevy::prelude::*;

pub struct GameHudPlugin;

impl Plugin for GameHudPlugin {
    fn build(&self, app: &mut App) {
        // Enhanced HUD will be added here
        // For now, this ensures the module compiles
    }
}
